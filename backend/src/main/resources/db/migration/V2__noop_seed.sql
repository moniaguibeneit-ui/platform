-- V2 - Seed: default ADMIN role placeholder (roles are created per-tenant via API)
-- No structural changes needed; the schema from V1 already supports RBAC.
-- This migration exists to keep Flyway history consistent for future additions.
SELECT 1;
